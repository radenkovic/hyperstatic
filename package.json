{
  "name": "hyperstatic",
  "version": "1.0.0",
  "description": "Generate Hyper Static HTML Websites on the fly",
  "main": "index.js",
  "scripts": {
    "start": "rimraf src/public && gulp",
    "build": "rimraf dist && gulp build",
    "docs": "rimraf dist && rimraf docs && npm run build && gulp docs"
  },
  "author": "Dan Radenkovic",
  "license": "MIT",
  "dependencies": {
    "browser-sync": "^2.18.13",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^4.0.0",
    "gulp-changed": "^3.1.0",
    "gulp-concat": "^2.6.1",
    "gulp-htmlmin": "^3.0.0",
    "gulp-imagemin": "^3.4.0",
    "gulp-inline": "^0.1.3",
    "gulp-inline-fonts": "^1.0.4",
    "gulp-minify-css": "^1.2.4",
    "gulp-nunjucks-html": "^2.0.0",
    "gulp-sass": "^3.1.0",
    "gulp-uglify": "^3.0.0",
    "rimraf": "^2.6.2"
  }
}
